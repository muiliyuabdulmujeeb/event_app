export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-panel" aria-labelledby="app-title">
        <p className="eyebrow">Phase 15 Setup</p>
        <h1 id="app-title">Event Management App</h1>
        <p className="hero-copy">
          The frontend runtime has been normalized for the upcoming implementation phases. Shared
          application structure, typed API integration, and workflow pages will be added in the
          next steps.
        </p>
      </section>
    </main>
  );
}
